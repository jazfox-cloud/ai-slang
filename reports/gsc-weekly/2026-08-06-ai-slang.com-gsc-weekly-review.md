# ai-slang.com GSC Weekly Review

数据日期

- Property `sc-domain:ai-slang.com` 可访问，权限级别：`siteFullUser`。
- 最近可用 GSC 数据日期：`2026-08-04`。
- 最近完整 7 天：`2026-07-29` 至 `2026-08-04`。
- 前 7 天：`2026-07-22` 至 `2026-07-28`。
- 最近完整 28 天：`2026-07-08` 至 `2026-08-04`。
- 前 28 天：`2026-06-10` 至 `2026-07-07`。

周期变化

- 7 天对比：clicks `0 vs 0`；impressions `584 vs 273`（`+113.9%`）；CTR `0% vs 0%`；average position `47.93 vs 50.22`（改善 `2.29` 位）。
- 28 天对比：clicks `2 vs 0`；impressions `1159 vs 123`（`+842.3%`）；CTR `0.17% vs 0%`；average position `45.65 vs 53.54`（改善 `7.88` 位）。前 28 天 clicks 为 `0`，这更像“开始出现真实点击”，不能当作稳定 CTR 增长。
- 最近 7 天主要 queries 已从上周的 `agentic` 集群转向更广义的 AI 术语/概念词：
  `what is geo` `30`、`vibe coding definition` `26`、`llm discoverability` `16`、`geo meaning` `15`、`slop meaning` `12`、`vibe coding meaning` `12`、`ai slang meaning` `10`。
- 最近 7 天主要 pages：
  `/terms/geo` `97`、`/terms/vibe-coding` `81`、`/terms/slop` `67`、`/articles/ai-website-builder-code-export` `43`、`/terms/tapestry` `37`、`/terms/gpu-rich-gpu-poor` `35`、`/articles/what-is-ai-slang` `32`、`/terms/delve` `32`。
- 最近 7 天上涨最明显的 queries：
  `what is geo` `+29` impressions、`geo meaning` `+14`、`vibe coding definition` `+13`、`llm discoverability` `+7`、`gpu rich` `+6`、`ai meaning slang` `+6`。
- 最近 7 天下滑最明显的 queries：
  `sycophancy` `-10` impressions、`sycophancy meaning` `-8`。在站点总量仍低、且 7 天 clicks 仍为 `0` 的前提下，不升级为异常。
- 最近 7 天上涨最明显的 pages：
  `/terms/geo` `+92` impressions、`/articles/ai-website-builder-code-export` `+43`、`/terms/vibe-coding` `+37`、`/terms/tapestry` `+24`、`/terms/gpu-rich-gpu-poor` `+24`、`/terms/ai-content-disclosure` `+23`、`/articles/what-is-ai-slang` `+18`。
- 最近 7 天下滑最明显的 pages：
  `/terms/sycophancy` `-30`、`/terms/speculative-decoding` `-11`。仍属低样本轮动。
- 最近 28 天上涨最明显的 queries：
  `vibe coding definition` `+34`、`ai slang meaning` `+34`、`vibe coding meaning` `+31`、`what is geo` `+31`、`llm discoverability` `+25`、`slop meaning` `+20`。
- 最近 28 天下滑最明显的 query 仍是 `agentic meaning`：`3 vs 17` impressions（`-14`）。
- 最近 28 天主要 pages：
  `/terms/vibe-coding` `135`、`/terms/slop` `113`、`/articles/what-is-ai-slang` `111`、`/terms/geo` `105`、`/terms/tapestry` `60`、`/terms/gpu-rich-gpu-poor` `57`、`/terms/delve` `55`、`/terms/sycophancy` `55`。
- 最近 28 天主要 page 下滑仍集中在 `/terms/agentic`：`23 vs 53` impressions（`-30`）。
- 最近 7 天 country 以 `USA` 为主：`230/584` impressions（`39.4%`），其次 `GBR` `44`、`IND` `42`、`PHL` `19`、`AUS` `18`、`THA` `18`。
- 最近 7 天 device 以 `DESKTOP` 为主：`505/584` impressions（`86.5%`）；`MOBILE` `74`、`TABLET` `5`。`MOBILE` average position `21.04` 明显好于 `DESKTOP` 的 `51.74`，但 clicks 仍为 `0`。
- `searchAppearance` 两个 7 天窗口都无数据行。

新问题

- 无新的技术异常。
- 无新的 sitemap 报错、抓取失败、canonical 冲突或 inspection FAIL。

持续问题

- 站点仍处于低点击、低 CTR 阶段。最近 7 天 clicks 仍为 `0`，最近 28 天也仅 `2` 次 click，暂时不宜把单周 query/page 轮动解读为稳定趋势。
- sitemap 仍显示 `submitted=59, indexed=0`；但 `warnings=0`、`errors=0`、`lastDownloaded=2026-07-17T16:32:54.472Z`，且与本周多个 `PASS / Submitted and indexed` inspection 冲突，继续只视为滞后信号，不单独判故障。
- `agentic` 集群仍在持续回落：`agentic meaning` 28 天 `17 -> 3` impressions，`/terms/agentic` 28 天 `53 -> 23` impressions；目前更像站内曝光重心转移，而不是索引故障。

已恢复

- 上周仍为 `URL is unknown to Google` 的三个重点 URL 已恢复为正常索引状态：
  `/terms/agentic`、`/terms/ai-content-disclosure`、`/terms/grok-build` 本周全部为 `PASS`、`Submitted and indexed`、`pageFetchState=SUCCESSFUL`、Google canonical = user canonical。

已完成等待刷新

- `AI content disclosure` 已发布且无需重做；本周 inspection 已转为 `PASS / Submitted and indexed`，该事项可视为完成。
- `grok-build` 本周 inspection 也已转为 `PASS / Submitted and indexed`，上周的等待刷新状态已结束。
- 旧 `.html` URL 仍在 GSC page rows 中出现，例如 `/articles/ai-slang-for-indie-hackers.html`、`/articles/gpt-ese-words-to-avoid.html`；仓库 [`_redirects`](/Users/jazfox/Documents/aislang/_redirects) 仍保留 `/articles/:slug.html /articles/:slug 301` 规则，因此继续标记为“已完成等待 GSC 刷新”，不单独升级为故障。

内容增长机会

- 继续优先做 AI 黑话、术语和概念解释，不扩展普通软件比较页。
- 当前最值得跟进的术语/概念集群是：
  `geo`、`vibe coding`、`slop`、`llm discoverability`、`gpu-rich gpu-poor`、`tapestry`、`delve`。
- `/articles/what-is-ai-slang` 28 天达到 `111` impressions、排名约 `27.01`，仍是最强的总览型内容入口，适合继续承接术语内链，而不是转去做泛软件选型页。
- `ai slang meaning`、`ai meaning slang` 两组 query 28 天分别达到 `35` 和 `21` impressions，说明站点的“AI slang / meaning”核心定位开始形成。若单独开内容任务，优先补强 glossary hub 与术语页之间的解释链路。
- `/articles/ai-website-builder-code-export` 与 `/articles/lovable-vs-bolt-code-export` 本周有曝光，但它们不属于本项目建议扩张方向；不据此建议增加普通软件比较页。

数据不足

- 7 天 clicks 仍为 `0`，28 天 clicks 也只有 `2`；CTR 和 query winner/loser 的业务意义仍弱。
- 最近 28 天虽显著高于前 28 天，但前周期基线只有 `123` impressions，增幅百分比会被低基数放大。
- `searchAppearance` 无数据，暂时无法判断富结果或展示类型变化。
- 一些上涨/下滑仍停留在十几次 impressions 量级，不应按严重异常处理。

P0/P1/P2/P3

- P0：无。
- P1：无。
- P2：无明确技术或索引故障需要修复。
- P3：
  - 继续观察 `agentic` 集群是否只是被新术语话题分流。
  - 继续观察旧 `.html` URL 在 GSC page rows 中的残留是否自然消退。
  - 若后续进入独立项目任务，优先做 glossary/术语页内链与解释深度优化，不扩普通软件比较页。

是否需要进入项目修复

- 否。
- 当前没有证据支持进入技术修复。
- 如需单独建议，仅建议后续排一个小范围内容任务：围绕 `geo`、`vibe coding`、`slop`、`llm discoverability` 与 `/articles/what-is-ai-slang` 做术语页与总览页的内链/解释强化；不执行本次任务内的任何修改。
